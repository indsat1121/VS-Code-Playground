[project]
name = "hello-world-python"
version = "0.1.0"
description = "A minimal hello world Python project."
requires-python = ">=3.9"

[project.scripts]
hello-world = "hello_world.main:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
